apiVersion: v1
kind: Service
metadata:
    name: vmfernandezgpipelinesdotnetcoredocker
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: vmfernandezgpipelinesdotnetcoredocker